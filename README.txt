All requests to the server are routed via the routes.js file into their respective callback functions that are
defined in the controller.

Controller has definitions of callback functions. These functions interact with the database models defined
in the User and UserSession model files. 